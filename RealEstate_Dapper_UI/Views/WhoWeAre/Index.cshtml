@model List<ResultWhoWeAreDetailDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
}

<div class="col-12">
    <div class="bg-light rounded h-100 p-4">
        <h6 class="mb-4">Biz Kimiz? Listesi</h6>
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Başlık</th>
                        <th scope="col">Alt Başlık</th>
                        <th scope="col">Açıklama 1</th>
                        <th scope="col">Açıklama 2</th>
                        <th scope="col" class="text-end"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@item.WhoWeAreDetailID</th>
                            <td>@item.Title</td>
                            <td>@item.SubTitle</td>
                            <td>@item.Description1</td>
                            <td>@item.Description2</td>

                            <td class="text-end">
                                <div class="dropdown">
                                    <a class="btn btn-sm btn-icon-only text-dark" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <i class="fa fa-ellipsis-v"></i>
                                    </a>
                                    <div class="dropdown-menu dropdown-menu-right dropdown-menu-arrow">
                                        <a class="dropdown-item" href="/WhoWeAre/UpdateWhoWeAreDetail/@item.WhoWeAreDetailID"><i class="fa fa-pen mx-0"></i> Güncelle</a>
                                        <a class="dropdown-item" href="/WhoWeAre/DeleteWhoWeAreDetail/@item.WhoWeAreDetailID"><i class="fa fa-trash mx-0"></i> Sil</a>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <a class="btn btn-primary col-12" href="/WhoWeAre/CreateWhoWeAreDetail"><i class="fa fa-plus mx-0"></i> Yeni "Biz Kimiz?" Ekle!</a>
        </div>
    </div>
</div>